const score = true;


console.log(typeof score);

let scoreUpdated = Number(score);
console.log(typeof scoreUpdated);
console.log(scoreUpdated);



/*
        if we convert a boolean to number it will give output as either 1 or 0
         if we convert a null to number it will give output as  0
         if we convert a StringCharacters to number it will give output as NaN


*/ 

